/**
 * This is a module so we can use it outside of components, e.g. in actions (see actions/AppActions.js)
 */

import createHistory from 'history/lib/createBrowserHistory';
export default createHistory();
